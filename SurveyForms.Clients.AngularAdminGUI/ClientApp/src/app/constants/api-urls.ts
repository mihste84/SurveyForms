import { environment } from '../../environments/environment';

export class ApiUrls {
    public static FormArea = environment.serverUrl + '/api/formarea';
    public static Form = environment.serverUrl + '/api/form';
}